import { Button } from "@/components/ui/button"

function App() {
  return (
    <div className="flex items-center justify-center h-screen bg-red-500">
      <Button className="hover:bg-blue-800">Setup Tailwind, ShadCN</Button>
    </div>
  )
}

export default App